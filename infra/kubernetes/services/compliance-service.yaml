apiVersion: v1
kind: Service
metadata:
  name: compliance-service
  namespace: veridex
  labels:
    app: compliance-service
spec:
  ports:
    - port: 3006
      targetPort: 3006
      protocol: TCP
  selector:
    app: compliance-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-service
  namespace: veridex
  labels:
    app: compliance-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: compliance-service
  template:
    metadata:
      labels:
        app: compliance-service
    spec:
      containers:
        - name: compliance-service
          image: veridex/compliance-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
          envFrom:
            - secretRef:
                name: veridex-secrets
            - configMapRef:
                name: veridex-config
          env:
            - name: PORT
              value: "3006"
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: veridex-secrets
                  key: MONGO_COMPLIANCE_URI
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          # Startup probe - allows time for app initialization
          startupProbe:
            httpGet:
              path: /health/live
              port: 3006
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30  # 5s * 30 = 150s max startup time
          # Liveness probe - restart if process is unhealthy
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3006
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          # Readiness probe - only route traffic when dependencies ready
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3006
            periodSeconds: 5
            timeoutSeconds: 10
            failureThreshold: 3
---
# PodDisruptionBudget - ensure availability during updates
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: compliance-service-pdb
  namespace: veridex
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: compliance-service
